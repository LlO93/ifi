# 투자 IF — 화면 구성 및 레이아웃

작성일: 2026-09-16
업데이트: 2026-09-23
상태: 모바일 중심·PC 대응, 밝고 간결한 디자인 확정. MVP는 직접 입력으로 6페이지. 캡처는 다음 버전이며 13절에 확장안을 보관한다.

## 1. 몇 개의 화면을 만들 것인가

**확정 구성은 독립 페이지 6개, 주요 화면 상태 7개, 보조 UI 5종이다.** 입력은 한 화면에서 모두 진행한다. 로딩·오류·빈 목록은 각 화면의 상태이며 별도 페이지로 세지 않는다.

| ID | 독립 페이지 | 경로 제안 | 주요 화면 상태 |
|---|---|---|---|
| P01 | 둘러보기 홈 | /explore | 순위 목록 1개. 거래량·시총은 같은 배치 |
| P02 | 종목 검색 | /search | 검색 1개. 입력 전·결과 없음은 상태 처리 |
| P03 | 종목 상세 | /stocks/:id | 차트 / 관련 종목, 2개 |
| P04 | 가상 매수 입력 | /simulate/:draftId | 날짜·자금·투자금 통합 입력 1개 |
| P05 | 계산 결과·기록 상세 | /records/:id | 결과 1개. 새 계산과 저장 기록이 같은 레이아웃 사용 |
| P06 | 내 기록 | /records | 목록 1개 |

합계: 1 + 1 + 2 + 1 + 1 + 1 = **7개 주요 화면 상태**. 별도의 기록 상세 페이지를 추가하지 않는다.

보조 UI 5종: 날짜 선택, 보유 종목 검색·추가, 과거 자금 기록 불러오기, 복기 메모 편집, 삭제 확인. 휴장일 안내는 날짜 선택 UI 안의 상태로 다룬다. 모바일에서는 긴 입력 UI를 전체 높이 시트로, 짧은 선택은 바텀시트로 표현한다.

로그인·회원가입·온보딩·설정·세금 리포트·독립 자산 대시보드는 현 MVP 페이지 수에 포함하지 않는다. 계정 기능을 출시 범위에 넣으면 화면 수를 다시 산정한다.

## 2. 공통 레이아웃 — 모바일 우선

- 기준 폭 390px, 최소 320px. 좌우 여백 20px, 좁은 폭에서는 16px.
- 상단 바 최소 높이 56px. 본문 섹션 간격 24~32px, 같은 섹션 요소 간격 8~16px.
- 제목 24px, 섹션 제목 18px, 본문·입력 16px, 보조 정보 13px. 금액은 표 형식 숫자와 통화 기호를 함께 표시.
- 입력·버튼 터치 높이 최소 44px, 주요 버튼 52px. 색상 이외에 +/− 기호와 텍스트를 함께 사용.
- 하단 탐색은 `둘러보기` / `내 기록` 2개. 홈·기록 목록에 표시한다.
- 종목 상세·검색·계산 입력에서는 하단 탐색을 숨긴다.
- 종목 상세는 본문과 분리된 화면 맨 아래 고정 영역에 ‘이날 샀다면?’ 버튼만 둔다. 홈·내 기록 메뉴는 없다. 버튼 영역은 safe-area를 포함하고 본문 스크롤과 독립적으로 항상 보인다.
- 결과 화면은 하단 탐색을 표시하고, 메모·재계산 버튼은 결과 본문 안에 배치한다. 고정 버튼과 하단 메뉴가 세 겹으로 쌓이지 않게 한다.
- 키보드가 열리면 필드·오류·계산 버튼을 가리지 않도록 스크롤하고, 하단 탐색은 노출하지 않는다.
- 로딩 시 기존 레이아웃을 유지한다. 숫자가 없으면 0 대신 ‘—’를 표시한다.

## 3. P01 둘러보기 홈

위에서 아래 순서:

1. 앱인토스 등록 이름 ‘그 주식 샀다면’과 페이지 제목 ‘둘러보기’. 기존 프로젝트명은 ‘투자 IF’다.
2. 전체 폭 검색 진입 영역: ‘종목명 또는 티커 검색’.
3. 순위 제목 ‘미국 주식 순위’와 데이터 기준일.
4. 거래량 / 시가총액 탭.
5. 순위 목록: 첫 화면에 약 5~6개 행이 보이는 밀도.
6. 하단 ‘더 보기’와 둘러보기 / 내 기록 메뉴.

순위 행은 약 76px: 왼쪽 순위(24px), 가운데 종목명·티커, 오른쪽 가격·등락률. 순위 지표는 종목명 아래 보조 행에 표시한다. 종목명은 최대 2줄, 금액은 줄바꿈하지 않으며 긴 금액은 목록에서 축약한다.

첫 진입에서 거대한 소개 배너·자산 입력·로그인 모달을 띄우지 않는다. 검색을 누르면 P02, 종목을 누르면 P03으로 이동한다. 홈으로 돌아오면 순위 탭·스크롤 위치를 보존한다.

## 4. P02 종목 검색

1. 뒤로 버튼 + 자동 포커스 검색창 + 입력 지우기.
2. 검색 전에는 최근 검색 종목(기기에 저장, 최대 5개), 없으면 안내 한 줄.
3. 검색 결과는 종목명·티커·거래소·지원 여부. 가격은 조회된 경우에만 보조 표시.
4. 결과 선택 시 P03으로 이동.

검색 타이핑 시 300ms 지연 후 조회하고, 이전 검색 응답이 나중에 도착해 최신 결과를 덮지 않게 한다. 검색 결과 없음에는 입력어 수정 안내를 제공한다.

보유 종목을 추가할 때는 동일 검색 컴포넌트를 O02 안에서 재사용한다. 이때 선택은 종목 상세 이동이 아니라 입력 폼에 종목 추가다.

## 5. P03 종목 상세

공통 상단:

1. 왼쪽 위 뒤로 화살표(최소 44px 터치 영역) / 회사 이름·티커. 상단 바는 고정한다.
2. 최근 종가 크게 표시, 전일 대비 등락률, 기준일 작게 표시.
3. 차트 / 관련 종목 탭.

### 차트 탭

4. 가격 차트 약 220px. 터치한 지점의 날짜와 종가 표시.
5. 기간 선택: 1개월 / 6개월 / 1년 / 전체.
6. 선택 날짜 행: ‘2025.04.07 · $70.00’ + ‘날짜 변경’.
7. 관련 종목 미리보기 최대 3개 + ‘관련 종목 더 보기’. 전체 관련 목록은 탭에서 제공.
8. 하단 고정 ‘이날 샀다면?’.

차트는 탐색을 위한 것이므로 선택하지 않은 날짜를 자동으로 과거 매수일로 확정하지 않는다. 날짜 미선택 상태에서 버튼을 누르면 P04 상단 날짜 필드에서 선택한다.

### 관련 종목 탭

4. 업종명과 관계 설명: ‘같은 반도체 업종’.
5. 세로 목록 최대 10개. 회사명·티커·가격·등락률·연결 이유 표시.
6. 선택한 종목 상세로 이동. 뒤로 가면 출발 종목의 탭과 스크롤을 복원.

관련 종목 탭에서 가상 매수 버튼은 상단에 표시된 현재 종목을 대상으로 한다. 버튼에 티커를 포함해 ‘NVDA 이날 샀다면?’처럼 대상을 명확히 한다.

확정: 차트 아래 관련 종목 미리보기와 별도 관련 종목 탭을 함께 제공한다.

### 진입·복귀와 화면 쌓기 — 사용자 확정

- 목록 또는 검색 결과에서 종목을 선택하면 상세가 오른쪽에서 왼쪽으로 들어와 이전 화면을 덮는다. 아래에서 올라오는 바텀시트가 아니라 가로로 쌓이는 전체 화면이다.
- 이전 화면은 뒤에 보존한다. 종목 상세에는 둘러보기·내 기록으로 바로 가는 메뉴를 두지 않는다.
- 상단 왼쪽 화살표를 누르면 현재 상세가 오른쪽으로 빠져나가고 직전 화면이 나타난다. 탐색 순서를 한 단계만 되돌린다.
- 예: 순위 → A 상세 → 관련 B 상세 → 뒤로 → A 상세 → 뒤로 → 순위.
- 복원 대상은 목록 스크롤, 검색어·검색 결과, 순위 탭, 종목 상세 탭, 차트 기간·선택 날짜다. 뒤로가기를 새 홈 진입으로 처리하지 않는다.
- 왼쪽 가장자리 약 24px에서 시작하는 오른쪽 스와이프를 지원한다. 드래그 중 상세가 손가락을 따라 이동하고 이전 화면이 드러난다. 차트 중앙에서 하는 드래그는 뒤로가기와 구분한다.
- 완료 기준 제안: 화면 폭의 28% 이상 이동하거나 최소 48px를 빠르게 이동. 부족하면 현재 화면 위치로 돌아오며, 복귀 스택은 변경하지 않는다.
- 세로 스크롤·취소된 터치에서는 뒤로가기를 실행하지 않는다. 화면 전환 중 추가 탭은 중복 처리하지 않는다.
- 진입·이탈은 약 260ms. 이전 화면은 약 18%만 좌우 이동해 깊이감을 준다. 동작 줄이기 설정에서는 슬라이드 애니메이션을 생략한다.
- 상세 페이지 구성은 고정 상단 바 / 독립 스크롤 본문 / 고정 하단 매수 버튼이다. 하단 버튼은 본문을 끝까지 내리지 않아도 보인다.
- 하단 버튼은 해당 상세 화면에 속한다. 화면이 닫힐 때 함께 오른쪽으로 사라지고 이전 화면의 하단 영역이 복원된다.
- 실제 앱 구현에서는 브라우저·OS 뒤로가기와 화면 스택을 연결해 같은 경로로 복귀한다. 브라우저 기본 가장자리 스와이프와 커스텀 동작이 동시에 발동하지 않도록 실행 환경별로 한 동작만 적용한다.
- 직접 URL로 상세에 들어와 이전 앱 화면이 없을 때는 뒤로 화살표가 둘러보기로 돌아가는 기본 경로를 제공한다.
- 아래에 가려진 화면은 키보드 포커스·스크린리더 탐색에서 제외한다. 복귀 후에는 이전에 선택했던 종목 행으로 포커스를 돌려준다.

## 6. P04 가상 매수 입력 — 한 화면 입력 확정

모든 조건을 한 페이지에서 위에서 아래 순서로 입력한다. 별도의 다음 단계 버튼은 두지 않는다.

1. **상단 바:** 뒤로 / ‘이날 샀다면?’ / 닫기. 대상 종목명·티커 표시.
2. **날짜:** 과거 날짜 필드 + 당시 종가. 차트에서 선택한 유효한 날짜를 미리 채운다. 휴장일이면 다음 거래일 제안 후 적용.
3. **자금 원천:** 예수금 / 주식 정리 / 둘 다. 바로 아래 ‘과거 자금 기록 불러오기’ 링크.
4. **자금 입력:** 예수금이면 해당 날짜의 달러 금액, 주식 정리이면 보유 종목 목록. 혼합은 둘 다 표시.
5. **보유 종목 카드:** 이름·티커, 당시 보유 수량, 팔 수량·전량 버튼, 당시 종가, 예상 매도 대금. 카드 추가·삭제 지원.
6. **가용 자금:** 예수금 + 매도 대금을 합산해 즉시 갱신. ‘이 날짜의 자금 기록으로 저장’ 선택 항목 배치.
7. **투자금:** 100% / 50% / 30% / 직접 입력. 선택 예산과 예상 매수 수량을 함께 표시.
8. **거래 요약:** 정리할 기존 주식, 새로 살 주식, 남은 기존 주식, 남는 현금. 상세 행은 접을 수 있지만 총액은 표시.
9. **하단 고정:** ‘현재 예상 잔고 계산하기’. 세금·수수료·배당 제외 문구는 버튼 위에 배치.

원천 선택에 필요한 입력만 보여준다. 예수금만 선택하면 주식 카드가 없어 화면이 짧아진다. 여러 종목을 추가하면 세로 스크롤하며, 입력 키보드와 계산 버튼이 겹치지 않게 처리한다.

유효하지 않은 입력이 있으면 필드 아래 원인과 수정 안내를 표시한다. 계산 버튼을 누르면 첫 오류로 이동한다. 계산 중에는 중복 제출을 차단하고 실패해도 입력값을 유지한다.

뒤로·닫기로 나가면 초안을 보존한다. 보유 수량은 과거 날짜 당시의 수량이며 매입단가는 요구하지 않는다. 투자 비중을 바꿔도 매도 수량은 자동으로 바꾸지 않는다.

자금 기록 저장을 선택한 경우 계산 요청과 함께 저장한다. 자금 기록 저장만 실패한 경우에는 시나리오 저장 성공 여부와 구분해 안내하며 재시도를 제공한다.

## 7. P05 계산 결과·기록 상세

첫 계산 직후와 내 기록에서 다시 열었을 때 동일 화면을 사용한다.

1. 뒤로 / ‘계산 결과’ 또는 ‘기록 상세’ / 더 보기(삭제).
2. 대상 종목, 가상 매수일 → 평가 기준일, 저장 상태.
3. **현재 예상 잔고** 큰 금액. 보조로 시작 대비 손익·수익률.
4. 비교 영역: ‘그대로 유지’와 ‘선택을 바꿨다면’ 금액을 같은 크기로 나란히 표시. 아래 차이를 한 문장으로 표시.
5. 잔고 구성: 신규 주식 / 남은 기존 주식 / 현금.
6. ‘계산 조건 보기’ 접기 영역: 원래 현금, 매도 수량, 거래 가격, 예산, 실제 매수액, 평가일.
7. 복기 메모 영역. 빈 상태는 ‘그때 왜 관심이 갔나요?’와 작성 버튼, 작성 후에는 메모 앞부분과 수정 버튼.
8. ‘조건 바꿔 계산’ / ‘최신 기준으로 다시 계산’ 보조 버튼.
9. ‘관련 종목 둘러보기’ 텍스트 링크.

상단 금액은 입력 자산 범위의 평가액이다. 바로 아래 간단히 범위·미반영 항목을 안내한다. 최신 기록을 열어도 당시 평가일을 유지한다. 재평가 이력은 날짜 선택 드롭다운으로 전환한다.

예시 레이아웃 수치는 MVP 명세의 고정 데이터 사용: 예상 잔고 $2,720, 시작 대비 +$720(+36%), 유지 $2,200, 선택 차이 +$520. 실제 시세가 아님.

확정: 현재 예상 잔고를 가장 크게 표시하고 유지 대비 차이는 바로 아래 비교 영역에 표시한다.

## 8. P06 내 기록

1. ‘내 기록’ 제목과 기록 개수.
2. 종목 검색 입력.
3. 전체 / 예수금 / 주식 정리 / 혼합 필터.
4. 최신 계산순 기록 카드.
5. 하단 탐색.

카드: 종목명·티커 / 가상 매수일·평가일 / 예상 잔고 / 유지 대비 차이 / 메모 한 줄. 같은 시나리오의 재평가는 목록에서 한 카드로 묶고 ‘평가 3개’처럼 표시한다. 카드 요약은 그 시나리오의 가장 최근 저장 평가를 사용하고 기준일을 함께 표시한다. 조건을 바꾼 복제는 별도 카드다.

빈 화면: ‘아직 계산한 기록이 없어요’ + ‘종목 둘러보기’. 로그인 전 상태에서는 ‘이 브라우저에서 만든 기록’ 안내를 보여준다. 새 계산이 자동 저장되며 목록에서 저장 동작을 다시 요구하지 않는다.

## 9. 보조 UI 5종

| ID | UI | 내용과 완료 동작 |
|---|---|---|
| O01 | 날짜 선택 | 달력 + 직접 날짜 입력, 종가 제공 범위 제한, 휴장일이면 다음 거래일 제안 후 적용 |
| O02 | 보유 종목 추가 | 검색 → 종목 선택 → 기존 자금 입력 화면에 카드 추가. 선택 후 수량 입력으로 포커스 이동 |
| O03 | 과거 자금 기록 | 기준일 이전 기록 목록 → 현금·보유량 미리보기 → ‘이 기록 적용’. 기존 입력을 대체한다는 설명 포함 |
| O04 | 복기 메모 | 당시 관심 이유 / 실행하지 않은 이유 / 지금의 생각, 각 1,000자 이내. 저장·취소 |
| O05 | 삭제 확인 | 대상 종목·매수일 표시, 삭제·취소. 삭제 후 목록 이동과 5초 실행 취소 |

O03 적용 후 실행 취소로 직전 입력 복구. O04 닫을 때 입력 초안을 보존하고 명시적으로 저장한 메모와 구분한다. 긴 시트는 제목과 완료 버튼만 고정하고 내용은 스크롤한다. 데스크톱에서는 같은 컴포넌트를 중앙 모달로 사용한다.

## 10. PC 대응

- 768px 미만: 1열 모바일 구성.
- 768~1,023px: 여백을 늘리되 1열 유지.
- 1,024px 이상: 좌측 탐색 200px, 본문 최대 1,120px. 하단 메뉴는 좌측 메뉴로 전환.
- 홈: 순위 표를 넓혀 가격·등락률·거래량 또는 시가총액을 독립 열로 표시.
- 종목 상세: 글로벌 좌측 탐색도 숨기고 이전 화면 위에 상세 화면을 쌓는다. 차트·관련 종목 본문과 하단 고정 매수 버튼을 분리한다. PC에서는 뒤로 화살표로 복귀한다.
- 입력: 본문 최대 680px. 오른쪽에는 거래 요약을 고정하되 입력 폼은 중복하지 않는다.
- 결과: 왼쪽 결과·비교·구성, 오른쪽 조건 요약·메모. 핵심 읽기 순서는 모바일과 동일.
- 기록: 표 또는 세로 카드 목록 유지. 화면을 채우기 위한 대시보드 통계를 추가하지 않는다.

## 11. 상태 및 이동 규칙

- 입력 오류: 필드 아래 원인·해결 안내. 계산 버튼 비활성화만으로 오류를 숨기지 않는다.
- 데이터 조회 실패: 섹션 내 재시도. 부분 실패 때문에 검색이나 뒤로 가기를 막지 않는다.
- 저장 실패: 결과 상단 ‘아직 저장되지 않았어요’ + 재시도. 성공 알림과 혼동 금지.
- 뒤로 가기: 검색어, 탭, 스크롤, 선택 날짜, 입력 초안 유지.
- 미지원 종목·기업행동: 조회 가능한 차트는 보여주고 가상 계산이 안 되는 이유와 다른 종목 탐색 진입 제공.
- 모든 버튼은 보이는 라벨 제공, 아이콘만 사용하는 뒤로·검색·더 보기는 접근성 이름 지정.
- 계산 완료 시 결과 제목에 포커스 이동. 오류 발생 시 첫 오류 필드에 포커스 이동.

## 12. 협의할 방향

확정: 모바일 중심, 밝고 간결한 스타일, 한 화면에서 직접 입력.
확정: 차트 아래 관련 종목 미리보기 + 관련 종목 탭, 결과의 가장 큰 숫자는 현재 예상 잔고.

답변이 없는 항목은 위 제안으로 표시해 두고, 확정된 결정과 섞지 않는다. 상세 계산 규칙과 데이터 계약은 [[MVP개발명세]]를 따른다.

## 13. 핑 반영 — 다음 버전의 캡처 입력 확장안

확정: 모바일 중심·PC 대응, 밝고 간결한 시각 스타일. 여백과 정보 위계를 중심으로 설계하며 다른 앱의 로고나 고유 화면을 그대로 복제하지 않는다.

사용자 결정: MVP는 직접 입력부터 구현하고, 캡처 자동 입력은 다음 버전으로 미룬다. 캡처는 메리츠 증권앱부터 지원하며 과거·현재 캡처를 모두 허용하고 기준 날짜를 확인한다. 아래 확장안은 MVP 개발 범위에 포함하지 않는다.

### 다음 버전에 캡처를 포함할 경우 화면 수

**독립 페이지 7개 / 주요 화면 상태 9개 / 보조 UI 5종.**

기존 6개 페이지에 P07 ‘잔고 캡처 가져오기’를 추가한다. P07에는 업로드와 인식 결과 확인이라는 2개 주요 상태가 있다. 인식 중·실패는 상태 변형으로 센다. P04는 한 화면 입력을 유지하고, 자금 입력 영역에서 캡처로 값을 채울 수 있게 한다.

### P07 잔고 캡처 가져오기 (/import/:draftId)

진입: P04 자금 입력 영역의 ‘잔고 캡처로 채우기’. 보조 진입은 같은 화면의 ‘저장한 자금 기록 불러오기’다.

업로드 화면 순서:

1. 뒤로 / 잔고 캡처 가져오기.
2. ‘예수금과 보유 수량이 보이는 화면을 올려주세요.’
3. 사진 선택 버튼. 여러 장 추가·삭제·교체 가능.
4. 선택 이미지 썸네일과 순서. 다른 계좌·날짜의 화면을 섞지 않도록 짧은 안내.
5. ‘이 잔고의 기준 날짜’ 입력. 이미지에서 날짜를 읽으면 제안하되 사용자 확인 전 확정하지 않음.
6. ‘내용 읽어오기’ 버튼.

인식 결과 확인 화면 순서:

1. ‘읽어온 내용을 확인해주세요.’
2. 기준 날짜와 확인 상태.
3. 예수금 금액·통화. ‘주문 가능 금액’과 ‘예수금’을 동일 값으로 임의 해석하지 않음.
4. 종목별 이름·티커 후보·보유 수량. 원본에 보이는 평가액은 참고용이며 과거 매수가나 보유 수량으로 대체하지 않음.
5. 불명확한 필드에 ‘확인 필요’ 표시. 원본 해당 이미지를 펼쳐 비교 가능.
6. 빠진 종목 추가, 잘못 읽은 행 삭제, 금액·수량 수정.
7. ‘확인한 내용 적용’ 버튼 → P04로 복귀. 매도 수량은 사용자가 별도로 선택.

원본 캡처에서 읽을 대상: 예수금·통화·종목명·보유 수량·명시된 기준일. 계좌번호·이름·전체 자산총액은 계산 입력에 필요하지 않으며 저장 모델에서 제외한다.

처리 규칙:

- 캡처가 없거나 읽기에 실패해도 직접 입력으로 계속할 수 있다.
- 인식 값은 초안이며 사용자 확인 없이 자금 기록이나 계산 결과로 확정하지 않는다.
- 여러 장에 같은 종목이 반복되면 합산하지 않고 중복 후보로 묶어 확인한다. 계좌가 다른 동일 종목도 임의 병합하지 않는다.
- 원화 평가액만 보이고 달러 예수금이 없으면 환율을 추정해 달러 예수금으로 채우지 않는다.
- 가려진 값·해독 불가 값은 공란으로 둔다. 0으로 채우지 않는다.
- 기존 P04 입력을 대체하기 전에 적용 내용 요약을 보여주고, 적용 취소 시 이전 입력을 복원한다.
- 현재 캡처를 과거 가정에 쓰면 ‘현재 잔고를 과거에도 보유했다고 가정’으로 표시한다. 실제 과거 보유 자산인 것처럼 기록하지 않는다.
- 이미지 내 기준일과 사용자가 지정한 날짜가 다르면 수정 또는 가정 적용을 명시적으로 선택한다.
- 화면의 시가총액·평가손익·평균단가를 보유 수량으로 잘못 매핑하지 않도록 지원 앱별 고정 예시로 검증한다.
- 초안 업로드 제한 제안: 이미지 5장, 각 10MB. 형식은 PNG·JPEG를 기본으로 하고 모바일 HEIC는 변환 지원 여부를 구현 단계에서 결정한다.
- 원본 이미지는 인식·확인을 위한 임시 자료로 처리하고 기록에는 확정한 수치만 남긴다. 실제 보존 기간과 외부 인식 서비스의 처리 정책은 공급자 선정 후 확정하며, 확인되지 않은 즉시 삭제를 UI에서 약속하지 않는다.

추가 개발 항목: 이미지 업로드·유효성 검사, 인식 서비스 연결, 종목 매핑, 인식 초안·수정 UI, 임시 이미지 삭제 정책. 기존 시세 기반 계산 엔진은 그대로 사용한다.

## 14. 디자인 시안의 범위

대화에 첨부한 시안에서는 6개 페이지 전환, 차트·관련 종목 탭 전환, 한 화면 자금 입력, 예수금·주식 정리 선택, 복기 메모 펼치기를 확인할 수 있다. 금액·종목·차트는 고정 예시이며 실제 계산·시세 연결·저장을 구현한 앱이 아니다. 한 화면 입력, 차트 아래 관련 종목 미리보기와 탭, 현재 예상 잔고 우선 강조를 반영했다.

시안 후속 반영: 상세 화면 스택, 상단 뒤로 화살표, 가장자리 스와이프, 하단 고정 매수 버튼을 추가했다. 시안에서는 앱 내부 이동만 재현하며 브라우저 히스토리 연결은 실제 앱 구현 범위다.

## 15. 기존 index.html을 참고한 디자인 발전

기존 시안의 파란색 #3182f6, 옅은 배경 #f8fafc, 짙은 본문 #191f28, 흰색 카드와 가는 테두리를 현재 6페이지 시안에 반영한다.

- 공통: IF 브랜드 마크, 파란 선택 상태, 8~12px 모서리, 절제된 그림자와 충분한 여백.
- 탐색: 상단 질문형 제목과 넓은 검색창, 종목 이니셜·이름·티커·가격이 정렬된 목록.
- 상세: 가격의 시각적 강조, 얇은 격자·영역 채움·선택 지점이 있는 파란 차트. 관련 종목에서 자기 자신은 제외.
- 입력: 날짜·자금·투자금 섹션 제목과 입력 필드 스타일을 통일.
- 결과: 현재 예상 잔고를 파란 카드로 강조, 아래에 유지 가정과 변경 가정을 비교. 이익은 녹색과 + 기호를 함께 사용.
- 기존의 종목 상세 화면 쌓기, 뒤로 화살표·가장자리 스와이프, 하단 고정 매수 버튼은 유지.

참고 파일의 세금·원화 혼합 계산·다단계 매매 기능은 현재 MVP 범위에 추가하지 않는다. 원본 index.html·style.css·app.js는 디자인 참고 자료로 보존하고 대화의 시안을 발전시킨다.

## 16. 다음 화면 작업 — 2026-09-23

사용자 결정: 앱인토스 앱 생성 이후에도 기획·화면 작업을 계속한다. 현재 작업 대상은 루트의 `index.html`, `style.css`, `app.js`, `js/components.js`다. 기존 6페이지 구성을 유지하며, 아래는 작업 우선순위 제안이다. 이 목록은 구현 완료 기록이 아니다.

| 순서 | 대상 | 구체적인 화면 작업 | 확인 기준 |
|---|---|---|---|
| 1 | 홈·종목 상세 | 등록 이름 반영, 데모 데이터 안내, 차트 기간·날짜 선택과 ‘이날 샀다면?’ 연결 | 종목과 선택 날짜가 입력 화면까지 이어지고, 뒤로 돌아와도 선택이 유지된다. 날짜 미선택 시 날짜 입력을 안내한다. |
| 2 | 가상 매수 입력 | 예수금만 입력하는 흐름부터 정리한 뒤 주식 정리·혼합으로 확장. 자금과 투자금, 남는 현금을 구분 | 사용자에게 필요한 필드만 보이고, 빈 입력·음수·보유량 초과·예산 초과의 원인과 수정 방법이 표시된다. |
| 3 | 결과 비교 | 현재 예상 잔고, 유지 가정과의 차이, 주식·현금 구성을 같은 조건으로 설명 | 매수일·평가일·입력 자산 범위가 보이고 손해인 경우도 같은 위계로 읽힌다. 고정 예시 결과를 실제 입력으로 계산한 결과처럼 표시하지 않는다. |
| 4 | 복기·내 기록 | 메모 작성·수정, 기록 없음·저장 실패 상태와 기록 재진입 설계 | 실제 저장 전에는 저장 완료를 약속하지 않는다. 기록을 다시 열 때 어떤 조건과 평가일의 결과인지 알 수 있다. |
| 5 | 전체 흐름 | 320px·390px·PC 폭, 뒤로가기·키보드·하단 버튼 겹침 점검 | 종목 탐색부터 복기까지 막힘 없이 이동하고, 데모와 실제 기능을 구분할 수 있다. |

우선 검토할 사용 예: 홈에서 종목 선택 → 차트의 과거 날짜 선택 → 예수금 입력 → 투자금 선택 → 유지/변경 결과 비교 → 당시 생각 작성. 첫 흐름을 확인한 뒤 기존 주식을 일부 팔아 투자하는 경우를 확인한다. 예시 금액과 계산 조건은 [[MVP개발명세]]의 검증용 예시를 따른다.

토스 전용 로그인·계좌 연동·SDK 적용은 이번 화면 작업에 자동으로 포함하지 않는다. 출시 환경 요구사항이 확인되면 별도 범위를 정한다.

### 2026-09-23 구현 기록

- 등록 이름 ‘그 주식 샀다면’을 화면에 반영하고 데모 안내를 표시했다.
- 가상 차트의 점·날짜 목록·기간 버튼을 연결했다. 가상 데이터에 있는 일부 날짜만 선택 가능하며 실제 거래일 달력은 아니다.
- 선택 종목·날짜, 예수금·보유 주식 A·매도 수량, 투자 비율·직접 입력 금액으로 데모 잔고를 계산한다. 주식 A의 매도일/평가일 가격은 $100/$120로 고정된 예시다.
- 실제 저장처럼 보이던 예시 기록을 저장 준비 안내로 교체했다. 복기 편집·영구 저장·시세 연결·기업행동 처리는 남아 있다.
- 검증: 계산·입력 오류 8개 확인, 브라우저에서 두 번째 종목과 날짜 전달·결과·뒤로가기·입력 보존·음수 오류·320px 가로 넘침 확인 통과.

### 후속 구현 — 브라우저 기록과 복기

- 계산 성공 시 조건·결과를 현재 브라우저에 자동 저장한다. 같은 브라우저·주소에서 다시 볼 수 있지만 계정·서버 저장은 아니다.
- 내 기록에서 종목 검색과 자금 원천 필터, 기록 상세 재진입을 연결했다.
- 관심 이유·실행하지 않은 이유·현재 생각을 각각 1,000자까지 작성·수정한다. ‘접기’는 현재 화면의 초안을 유지하며, ‘메모 저장’을 눌러야 저장 기록을 바꾼다. 새로고침 전 미저장 초안은 보존하지 않는다.
- ‘조건 바꿔 새로 계산’은 원래 결과를 유지하면서 새 기록을 만든다. 저장 실패 시 결과·메모 초안을 유지하고 재시도 버튼을 제공한다.
- 로직 검증 통과: 자동 저장, 메모 수정·HTML 문자 처리, 기록 복원, 원본 보존, 검색·필터, 저장 실패·재시도, 모듈 재로딩 후 복원, 손상 데이터 덮어쓰기 방지.
- 이번 저장·메모 UI의 브라우저 검증은 자동 승인 검토가 워크스페이스 크레딧 부족으로 거절해 미확인이다. 이전 계산 흐름의 브라우저 검증과 구분한다.
- 남은 범위: 실제 시세·기업행동, 최신 시세 재평가, 계정·서버 저장, 저장·메모 UI 브라우저 검증.

### 후속 구현 — 기록 삭제·실행 취소

- 저장된 결과 상세에서 ‘이 기록 삭제’를 누르면 종목·가상 매수일과 메모도 삭제된다는 안내를 보여준다. ‘취소’는 기록을 변경하지 않는다.
- ‘삭제하기’가 성공하면 내 기록으로 이동하고 5초 동안 ‘실행 취소’를 제공한다. 같은 기록의 조건·결과·저장된 메모를 복원하며 중복 기록을 만들지 않는다.
- 삭제 실패 시 원래 기록을 유지하고, 실행 취소 저장 실패 시 남은 시간 안에 재시도할 수 있다. 여러 삭제는 각각의 취소 기한을 가진다.
- 실행 취소용 사본은 메모리에만 둔다. 5초가 지나거나 새로고침하면 실행 취소할 수 없다고 화면에 안내한다.
- 검증 통과: 삭제 저장 실패 시 보존, 선택 기록만 삭제, 메모 포함 복원, 복원 실패·재시도, 중복 실행 취소 방지, 5초 경계, 여러 기록 삭제·복원, JavaScript 문법.
- 브라우저 화면 검증은 이전에 확인된 로컬 파일 URL 접근 차단으로 미확인이다. 이번 검증은 저장소를 모사한 로직 검증이다.

### 실제 주가 조회 — 개인 무료 개발 검증

- 기존 6페이지 데모와 구분되는 ‘실제 시세 조회’ 탭을 추가했다. 개인 키가 없으면 공식 IBM 예제만 노출한다. 시작 목록은 순위가 아니다.
- 개인 키 설정 후 종목명·티커 검색과 일별 차트를 조회한다. 실시간 가격이 아닌 제공 마지막 종가·거래일·수집시각·출처·미조정 가격 기준을 표시한다.
- 최근 20/60거래일·제공 전체 차트, 날짜별 종가·거래량 선택을 제공한다. 최근 100개 자료 중 당일은 제외한다.
- 가상 종목 계산과 실제 시세를 섞지 않는다. 분할·기업행동 처리를 갖추기 전까지 실제 가격 기반 잔고 계산은 열지 않는다.
- 검증: 시세 파싱·오류·캐시·검색 필터·키 노출 방지 테스트 5개 통과. 실제 IBM 예제 API 99개 자료 수신, 기준일 2026-09-21. 로컬 HTTP 브라우저에서 차트 출력·20일 기간·날짜 선택 확인. 비밀 파일·서버 소스 HTTP 접근은 404 확인.
