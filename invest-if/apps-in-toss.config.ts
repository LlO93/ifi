import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'invest-if',
  brand: {
    primaryColor: '#5B4FD6',
  },
  permissions: [],
  webBundleDir: 'dist',
});
