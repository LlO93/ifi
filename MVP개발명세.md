# 투자 IF — MVP 개발 명세

작성일: 2026-09-16
상태: 구현 착수용 초안. 사용자 요청으로 확정한 범위와 아래 설계 제안을 구분한다.

## 1. 제품 정의

**종목을 둘러보다가 “그때 내 예수금이나 보유 주식 일부로 이 종목을 샀다면, 지금 얼마일까?”를 계산하고 당시의 판단을 기록하는 투자 복기 앱.**

단순 종목 수익률 계산기가 아니라, 내가 실제로 쓸 수 있었던 자금으로 선택을 바꾸는 경험이 핵심이다. 첫 화면은 자산 입력 화면이 아니라 종목 탐색 화면이다. 자산 기록 없이도 탐색하고, 계산할 때 필요한 정보만 입력한다.

### 사용자 요청으로 확정

- MVP에서 세금 계산 제외. 현재 예상 잔고를 중심으로 결과 제공.
- 과거 예수금으로 매수하거나, 당시 보유 종목 일부를 정리한 돈으로 다른 종목을 매수하는 가정 지원.
- 종목 진입 전에 순위로 탐색 가능.
- 개별 종목의 가격 차트와 관련 종목 탐색 제공.
- 계산한 시나리오를 내 기록으로 보관.
- 첫 출시 시장은 미국 주식.

### 개발을 위한 제안 기본값

- 미국 주식의 계산 통화는 USD로 제안한다. KRW 환산은 후속 범위이며, 원화와 달러를 그대로 합산하지 않는다.
- 모바일 우선 반응형 웹으로 구현하고, 네이티브 앱 배포는 후속 단계로 둔다.
- 시나리오 1개는 과거 하루의 매도·매수 1회와 현재까지 보유하는 가정이다.
- 여러 보유 종목을 매도 자금으로 선택할 수 있지만, 새로 매수하는 종목은 1개다.
- 정수 주식만 거래한다. 세금·수수료·배당·현금 이자는 반영하지 않는다.
- MVP의 ‘현재’는 가장 최근 완료된 정규 거래일 종가다. 모든 비교 종목에 동일한 평가일을 적용한다.
- 거래량·시가총액 순위와 동종 업종 관련 종목부터 제공한다. 실시간 인기·함께 검색된 종목은 충분한 데이터가 생긴 뒤 추가한다.
- 계산 성공 시 자동 저장한다. 복기 메모는 선택 사항이다.

## 2. MVP 범위

| 항목 | 첫 버전 | 후속 버전 |
|---|---|---|
| 종목 탐색 | 거래량·시가총액 순위, 종목명·티커 검색 | 실시간 인기, 개인화 추천 |
| 종목 상세 | 일봉 가격 추이, 기간 선택, 날짜 선택 | 분봉, 기술적 지표 |
| 관련 종목 | 동일 세부 업종, 없으면 동일 상위 산업 | 실제 함께 검색된 종목, 테마 관계 |
| 자금 설정 | 과거 현금 직접 입력, 보유 종목·수량 입력 | 증권사 연동, 자동 거래 내역 복원 |
| 가상 거래 | 같은 날 일부 매도 후 단일 종목 매수 | 다단계 매수·매도·재투자 |
| 결과 | 현재 예상 잔고, 손익, 유지했을 때와 차이 | 실제 계좌 전체와 비교, 원화 환산 |
| 내 기록 | 자동 저장, 메모, 필터, 복제, 삭제, 재평가 | 공개 공유, 복기 통계 |
| 세금·비용 | 제외 사실 표시 | 수수료·세금·배당 반영 |

‘실제 내 자산’과의 비교는 실제 거래 내역 없이는 구현하지 않는다. MVP 비교값은 **입력한 현금과 보유 주식을 그대로 유지했을 경우**다.

## 3. 대표 사용자 흐름

1. 탐색 홈에서 거래량 순위를 보고 관심 종목에 들어간다.
2. 차트에서 과거 날짜를 선택하거나 날짜 입력으로 이동한다.
3. ‘이날 샀다면?’을 누른다.
4. 자금 원천을 선택한다: `예수금으로` / `보유 주식 정리해서` / `둘 다`.
5. 그날 예수금과 보유 수량, 매도할 수량을 입력한다.
6. 가용 자금 중 투자 비중 또는 투자 금액을 정한다.
7. 예상 매수 수량과 남는 현금을 확인하고 계산한다.
8. 현재 예상 잔고와 기존 선택을 유지했을 때의 잔고를 비교한다.
9. 자동 저장된 기록에 ‘당시 관심을 가진 이유’, ‘매수하지 않은 이유’, ‘지금의 생각’을 적는다.
10. 관련 종목으로 이동해 새로운 시나리오를 계산한다.

첫 사용자는 현금 금액만 입력해도 계산할 수 있다. 자산 입력이나 메모 작성이 탐색을 막지 않는다.

## 4. 화면과 상호작용

하단 탐색: `둘러보기` / `내 기록`. 종목 상세에서는 하단 탐색을 숨기고 맨 아래 고정 ‘이날 샀다면?’ 버튼만 제공한다. 과거 자금 기록은 계산 입력 화면에서 관리한다.

### 4.1 둘러보기

- 상단 종목명·티커 검색. 한글 별칭 검색은 확보한 별칭 데이터 범위에서 지원.
- 순위 탭: `거래량`(기본) / `시가총액`.
- 행: 순위, 종목명, 티커, 종가, 전일 대비 등락률, 해당 순위 지표.
- 기본 50개, 페이지당 25개. 전체 검색은 순위 밖의 지원 종목도 검색한다.
- 거래량 순위는 최근 완료 거래일의 거래 주식 수 내림차순. 시가총액 순위는 공급자가 제공한 같은 기준일 값 내림차순. 동률은 티커 오름차순.
- 지원 시장·지원 종목군·데이터 기준시각을 목록 위에 표시한다. 첫 버전 종목군은 보통주로 제한하고 ETF 등은 검색 결과에서 지원 여부를 명시한다.
- ‘실시간’이라는 이름은 실시간 데이터 계약과 갱신 체계가 확보된 경우에만 사용한다.
- 로딩 스켈레톤, 검색 결과 없음, 조회 실패·재시도, 지연 데이터 표시를 구분한다.

### 4.2 종목 상세

- 종목명, 티커, 거래소, 통화, 최근 종가, 가격 기준일.
- 탭: `차트` / `관련 종목`.
- 차트 기간: 1개월 / 6개월 / 1년 / 전체 제공 기간.
- 차트 터치·클릭 시 날짜와 가격 표시. 날짜 입력을 대체 수단으로 제공.
- 고정 버튼: `이날 샀다면?`. 선택 날짜가 없으면 날짜 선택을 먼저 안내.
- 관련 종목 카드는 `같은 세부 업종` 또는 `같은 산업`이라는 연결 이유와 업종명을 표시한다.
- 동일 세부 업종 후보를 우선하고, 부족하면 상위 산업에서 보충한다. 자기 자신과 중복 종목 제외, 시가총액 내림차순 최대 10개.
- 업종 분류 데이터가 없으면 ‘관련 종목 정보가 아직 없어요’. 임의로 함께 검색된 종목이라고 표시하지 않는다.
- 관련 종목에서 뒤로 돌아오면 이전 차트 기간·선택 날짜를 복원한다. 계산 입력값을 다른 종목에 자동 적용하지 않는다.

### 4.3 ‘이날 샀다면?’ 입력

- 날짜: 사용자가 고른 날짜와 실제 적용 거래일을 함께 표시.
- 자금 원천에 따라 입력 필드를 펼친다.
- 현금: 해당 날짜의 예수금 C. 기본값은 공란이며 현재 현금을 대신 넣지 않는다.
- 보유 종목별: 종목, 당시 보유 수량 H, 매도할 수량 S. `전량` 단축 버튼 제공.
- 보유 수량은 선택한 과거 날짜 당시의 수량 기준이다. 과거 매입단가는 필요하지 않다.
- 매도 예상 금액은 해당 거래일 종가로 계산하며 사용자가 임의 가격으로 덮어쓰지 않는다.
- 투자 비중: 가용 자금의 100% / 50% / 30% / 금액 직접 입력.
- ‘가용 자금’은 입력 현금 + 선택한 매도 대금이다. 비중을 바꿔도 선택한 매도 수량은 자동 변경하지 않는다.
- 계산 전 요약: 매도 대금, 가용 자금, 투자 예산, 예상 매수 수량, 거래 후 현금.
- 안내: ‘선택한 날짜 종가로 매도·매수한 뒤 보유했다고 가정합니다.’
- 계산과 기록 저장 성공 후 결과로 이동. 저장 실패 시 결과는 보존하고 저장 재시도 제공.

#### 과거 자금 기록

입력 내용을 `이 날짜의 자금 기록으로 저장`할 수 있다. 현금과 종목별 보유 수량을 저장하며 매도할 수량은 시나리오에만 저장한다.

동일 날짜의 기록이 있으면 불러올 수 있다. 없다면 선택 날짜 이전의 가장 가까운 기록을 제안하되 ‘8월 1일 기록을 8월 12일에도 적용할까요?’처럼 원래 날짜를 표시하고 사용자 적용 후 편집하게 한다. 미래 기록은 제안하지 않는다. 월 1회 입력은 선택 사항이며, 월별 총자산만으로 과거 보유 수량을 추정하지 않는다.

### 4.4 결과

주요 카드:

1. **현재 예상 잔고**: 새로 산 주식 평가액 + 남은 기존 주식 평가액 + 남은 현금.
2. **기준일 대비 증감**: 시나리오 시작 시점의 자산 대비 손익과 수익률.
3. **그대로 유지했다면**: 입력한 주식과 현금을 유지한 가정의 현재 잔고.
4. **선택을 바꿨을 때 차이**: 가상 잔고 − 유지 가정 잔고.

보조 영역: 종목별 현재 수량·평가액, 현금, 실제 매수 투입금, 신규 매수 종목의 평가손익.

‘예상 잔고’ 아래에 ‘입력한 자산 범위의 평가액이며 출금 가능한 현금과 다릅니다. 세금·수수료·배당 미반영’ 표시. 다른 계좌나 입력하지 않은 자산은 포함하지 않는다.

행동: `복기 메모 작성`, `조건 바꿔 다시 계산`, `관련 종목 보기`.

### 4.5 내 기록

- 최신 계산순 목록. 대상 종목 검색, 자금 원천 필터.
- 카드: 대상 종목, 가상 매수일, 계산일, 당시 저장한 예상 잔고, 유지 대비 차이, 메모 유무.
- 상세에서 입력 조건과 계산 근거를 다시 볼 수 있다.
- 결과는 계산 당시 스냅샷으로 유지한다. 목록의 숫자를 매일 몰래 바꾸지 않는다.
- `최신 기준으로 다시 계산`은 기존 기록과 연결된 새 결과를 만든다. 이전 결과도 남긴다.
- 조건 변경은 원본을 복제해 새 시나리오 생성. 메모는 수정 가능.
- 삭제는 확인 후 수행하며 짧은 실행 취소 제공.
- 손익의 좋고 나쁨을 점수화하지 않고 당시 판단 메모를 함께 보여준다.

## 5. 계산 규칙

### 5.1 날짜·가격

- 거래소 현지 날짜로 가상 거래일을 해석한다. 저장 시 거래소·현지 거래일·UTC 수집시각을 구분한다.
- 휴장일을 선택하면 다음 거래일을 제안하고 적용 전에 확인받는다. 사용자의 날짜를 조용히 변경하지 않는다.
- 미래 날짜 및 정규장이 끝나지 않아 종가가 없는 당일은 선택 불가.
- 평가일 T는 가장 최근 완료된 거래일이다. 관련 종목 중 하나라도 T 가격이 없으면 이전 날로 조용히 혼합하지 않고 계산을 중단한다.
- 과거 매도·매수는 동일 거래일 d의 원시 종가를 사용한다. 매도 대금이 즉시 매수에 사용 가능하다고 가정한다.
- 배당 재투자가 포함된 수정주가를 매수가로 쓰지 않는다. 가격 종류를 공급자 데이터와 함께 저장한다.
- 주식 분할은 d 이후 T까지의 누적 분할 배수로 보유 수량에 반영한다. 차트도 가격 기준을 명시한다.
- 합병·분사·상장폐지 등 지원하지 않는 기업행동이 기간에 있으면 결과를 만들지 않고 계산 불가 사유를 표시한다. 데이터가 없는 종목을 0원으로 평가하지 않는다.

### 5.2 기호

- C: 기준일 현금. 주식 매도만 선택했다면 0.
- Hᵢ: 기존 종목 i의 기준일 보유 수량.
- Sᵢ: 같은 날 매도할 수량, 0 ≤ Sᵢ ≤ Hᵢ.
- Pᵢ,d / Pᵢ,T: 기존 종목의 기준일 / 평가일 원시 종가.
- Pₓ,d / Pₓ,T: 새로 매수할 종목의 기준일 / 평가일 원시 종가.
- Fᵢ / Fₓ: (d, T] 사이 누적 분할 배수. 분할이 없으면 1.
- B: 매수 예산. 가용 자금 이하의 금액.

```text
매도 대금 L = Σ(Sᵢ × Pᵢ,d)
가용 자금 A = C + L
매수 수량 Q = floor(B / Pₓ,d)
실제 매수액 K = Q × Pₓ,d
거래 후 현금 R = A − K
시작 자산 V₀ = C + Σ(Hᵢ × Pᵢ,d)
현재 가상 잔고 Vif = R + Q × Fₓ × Pₓ,T + Σ((Hᵢ − Sᵢ) × Fᵢ × Pᵢ,T)
유지 가정 잔고 Vhold = C + Σ(Hᵢ × Fᵢ × Pᵢ,T)
기준일 대비 손익 = Vif − V₀
기준일 대비 수익률 = (Vif − V₀) / V₀ × 100
선택 차이 = Vif − Vhold
신규 매수 종목 손익 = Q × Fₓ × Pₓ,T − K
```

- V₀ ≤ 0 또는 Q = 0이면 계산 불가. 1주를 사는 데 필요한 금액 안내.
- 동일 종목 행은 병합하고, 매수 대상과 매도 대상이 같으면 해당 매도 입력을 차단한다.
- 직접 입력 예산 B가 A를 넘으면 오류. 음수·NaN·무한대·빈 필수값도 거부.
- 입력 보유·매도 수량은 정수. 분할로 생긴 소수 수량은 평가에 유지하고 소수점 때문에 임의 현금 정산하지 않는다.
- 금액 계산은 decimal 계열 연산으로 수행하고 중간 계산에서 표시용 반올림하지 않는다. 화면은 통화 소수 자릿수로 표시한다.
- 현금만 입력한 경우 Vhold = C. ‘유지’는 이자 없이 현금으로 보유한 가정이다.

### 5.3 검증용 예시 — 실제 시세가 아닌 고정 테스트 데이터

현금 $1,000, 기존 A 종목 10주, 과거 A 가격 $100, 현재 A 가격 $120. A 4주를 팔고 B를 과거 가격 $70에 가용 자금 전액으로 매수. 현재 B 가격 $100, 분할 없음.

- 매도 대금 $400, 가용 자금 $1,400, B 20주 매수, 남은 현금 $0.
- 시작 자산 $2,000.
- 가상 현재 잔고 = B $2,000 + 남은 A $720 = **$2,720**.
- 유지 가정 = 현금 $1,000 + A $1,200 = **$2,200**.
- 시작 대비 손익 **+$720 / +36%**, 선택 차이 **+$520**.

## 6. 데이터 모델

금액·가격·수량은 DB의 decimal 또는 손실 없는 문자열 직렬화로 다룬다. 모든 사용자 데이터에 owner_id를 둔다.

| 엔티티 | 주요 필드 |
|---|---|
| Instrument | id, ticker, name, exchange, currency, type, industry_id, active_from, active_to |
| PriceDaily | instrument_id, trading_date, raw_close, volume, currency, provider, fetched_at, revision |
| CorporateAction | instrument_id, effective_date, type, ratio, provider, revision |
| RankingSnapshot | market, metric, as_of_date, generated_at, items(rank, instrument_id, metric_value) |
| AssetSnapshot | id, owner_id, effective_date, currency, cash, positions(instrument_id, quantity), created_at |
| Scenario | id, owner_id, target_id, requested_date, effective_date, source_mode, cash, positions(held_qty, sell_qty), budget_mode, budget_value, source_snapshot_id, parent_scenario_id, created_at |
| ScenarioEvaluation | id, scenario_id, evaluated_at, valuation_date, price_inputs, split_inputs, engine_version, data_revision, quantity_bought, cash_remaining, initial_value, hypothetical_value, hold_value, gain, gain_pct, difference, previous_evaluation_id |
| Reflection | scenario_id, interest_reason, decision_reason, hindsight_note, updated_at |

Scenario에는 참조 ID뿐 아니라 실제 적용한 자금 값을 저장한다.

자금 기록을 편집해도 과거 시나리오는 바뀌지 않는다. ScenarioEvaluation에도 사용한 가격과 분할 정보를 고정 저장해 당시 결과를 재현할 수 있게 한다.

## 7. 구현 구성과 API 계약

제안 구성: TypeScript 기반 반응형 웹 + 서버 API + PostgreSQL. 구체 프레임워크와 배포처는 개발 환경 선택 시 확정한다. 시세 공급자는 교체 가능한 어댑터로 분리한다.

- 시세 키는 서버에만 보관. 순위·가격·기업행동은 서버에서 수집하고 캐시한다.
- 순위와 검색은 로그인 없이 제공. 기록은 익명 사용자 세션으로 서버 저장하고 추후 계정 연결을 지원한다. 익명 상태에서는 같은 브라우저에서만 이어볼 수 있음을 명시한다.
- 세션은 HttpOnly 쿠키 기반으로 소유권 검사. 익명 식별자만으로 다른 사용자 기록을 조회할 수 없게 한다.
- 동기화·계정 복구는 계정 연결을 구현하기 전까지 제공한다고 약속하지 않는다.
- 계산 엔진은 UI·시세 API와 분리한 순수 함수로 구현한다.

| API | 요청·응답 핵심 |
|---|---|
| GET /rankings | market, metric, cursor → 기준일, 종목 목록, 다음 cursor |
| GET /instruments | query, market → 지원 여부 포함 검색 결과 |
| GET /instruments/:id/prices | from, to → 일별 가격·가격 기준·누락 상태 |
| GET /instruments/:id/related | → 관계 이유·업종 포함 종목 목록 |
| GET /asset-snapshots | on_or_before → 날짜가 표시된 자금 기록 |
| POST /asset-snapshots | 날짜, 현금, 보유 수량 → 저장 결과 |
| POST /scenarios | 입력 조건, idempotency_key → 시나리오 + 첫 평가 결과 |
| GET /scenarios | target, source_mode, cursor → 내 기록 목록 |
| GET /scenarios/:id | → 입력 조건, 평가 이력, 메모 |
| POST /scenarios/:id/evaluations | idempotency_key → 최신 기준 새 평가 |
| PATCH /scenarios/:id/reflection | 메모 필드 → 수정 결과 |
| DELETE /scenarios/:id | → 삭제 결과 |

- POST /scenarios는 입력 검증 → 가격 조회 → 계산 → 시나리오·평가 원자적 저장 순서. 더블 탭·재시도는 idempotency_key로 중복 방지.
- 오류 코드는 INVALID_INPUT, NON_TRADING_DATE, INSUFFICIENT_BUDGET, PRICE_MISSING, UNSUPPORTED_ACTION, DATA_UNAVAILABLE, SAVE_FAILED로 구분한다.
- NON_TRADING_DATE는 suggested_date를 반환하고 사용자 적용 후 재요청한다.
- 저장 실패 시 임시 계산 결과와 재시도 토큰을 반환할 수 있지만 ‘저장됨’ 상태는 성공 이후에만 표시한다.
- 목록은 페이지네이션. 요청 제한과 공급자 호출 캐시 적용.

## 8. 데이터 공급자 선정 및 출시 조건

이 문서는 특정 공급자의 지원 범위·가격·라이선스를 검증한 문서가 아니다. 실제 데이터 연결 전에 다음 항목을 확인한다.

- 지원 시장의 종목 목록, 원시 종가, 거래량, 시가총액, 업종 분류, 분할·기업행동 데이터 제공 여부.
- 사용자에게 시세를 표시하고 계산 당시 값을 저장할 수 있는 계약·보존 조건.
- 거래소 시간대·캘린더, 데이터 확정 시각, 수정 이력, 결측치와 상장폐지 상태 제공 여부.
- 확보한 과거 시세 기간을 UI 날짜 선택 범위에 그대로 반영.
- 동일 기준일 평가와 기업행동 확인이 불가능한 기간은 계산 대상으로 열지 않는다.
- 데이터 장애 시 마지막 성공 자료와 기준시각 표시. 최신인 것처럼 표시하지 않는다.

공급자 연결 전에는 고정 fixture로 전체 흐름을 개발할 수 있다. fixture를 쓰는 화면에는 ‘데모 데이터’를 표시하고 출시 데이터와 분리한다.

## 9. 구현 순서와 완료 기준

### 단계 1 — 계산 엔진과 데이터 계약

- Instrument·PriceDaily·CorporateAction 및 시세 어댑터 정의.
- 현금만 / 주식 매도만 / 혼합, 정수 매수, 잔여 현금, 유지 비교 구현.
- 위 $2,720 예시 검증.
- 예산 $1,000 / 주가 $90이면 11주와 현금 $10 검증.
- 2:1 분할 전 10주 × $100과 분할 후 20주 × $50의 자산 가치가 같은지 검증.
- 휴장일, 예산 초과, 보유량 초과, 시세 누락, 기업행동 미지원, 통화 불일치 오류 검증.

완료: 동일 입력·동일 가격·동일 엔진 버전은 항상 동일 결과를 낸다.

### 단계 2 — 탐색과 관련 종목

- 순위 2종, 검색, 상세 차트·날짜 선택, 업종 기반 관련 종목 구현.
- 순위 기준일과 차트 가격 기준 표시.
- 실제 공급자 선정 후 fixture와 교체.

완료: 홈 → 종목 → 관련 종목 → 이전 종목 복귀가 모바일에서 가능하고 데이터 실패 상태가 구분된다.

### 단계 3 — 시나리오 입력과 결과

- 자금 원천 3종, 보유 수량·매도 수량, 투자 예산, 가격 근거 표시.
- 예상 잔고·유지 가정·차이와 종목별 구성 구현.
- 과거 자금 기록 저장·명시적 불러오기 구현.

완료: 과거 기록이 없는 사용자도 현금 입력만으로 계산 가능. 일부 매도 후 남은 주식과 현금이 누락되지 않는다.

### 단계 4 — 기록과 복기

- 계산 자동 저장, 메모, 검색·필터, 복제, 재평가 이력, 삭제 구현.
- 중복 요청 방지, 소유권 검사, 저장 실패 복구 구현.

완료: 새로고침 후 기록이 유지되고, 재평가해도 이전 평가값은 보존된다. 다른 사용자의 기록 접근은 차단된다.

### 단계 5 — 출시 점검

- 대표 흐름을 실제 공급자 데이터로 통합 검증.
- 가격의 동일 기준일·분할 처리·결측치 차단 확인.
- 좁은 화면 터치, 키보드 날짜 입력, 색 이외의 손익 부호, 로딩·빈 화면·오류 확인.
- 세금·실제 전체 자산·실시간 가격으로 오해할 수 있는 문구 제거.
- 공급자 계약 및 저장·표시 조건 확인을 출시 조건으로 설정.

## 10. 기존 프로토타입과의 차이

현재 폴더의 index.html, app.js, style.css는 이전 세금 중심 방향을 보여주는 정적 프로토타입이다. 이 기획 작업에서는 코드를 변경하지 않았다.

구현 시 다음을 교체한다.

- 첫 화면의 세후 자산 비교 → 종목 탐색 홈.
- 세금 리포트·거래 원장·연속 재매매 메뉴 → 내 기록.
- 고정 actualAsset과 일부 투자액의 비교 → 동일 입력 자산의 유지 가정 비교.
- 하드코딩된 가격·cashMultiplier → 실제 가격 입력과 검증 가능한 계산식.
- 달러 주가와 원화 자산의 혼합 계산 → 단일 통화 계산.
- 월별 총자산 자동 적용 → 현금·종목별 수량을 사용자가 확인하고 적용.

## 11. 제품 검증

확인할 질문: 사용자가 종목을 탐색하다 계산을 완료하는가? 다른 종목까지 탐색하는가? 저장한 기록을 다시 열고 판단을 복기하는가?

이벤트: ranking_open, instrument_open, related_instrument_open, simulation_started, simulation_completed, simulation_failed, reflection_saved, history_reopened.

지표: 계산 시작 대비 완료율, 관련 종목 이동률, 메모 작성률, 7일 이내 기록 재방문율. 출시 후 기준선을 측정하고 목표치를 정한다. 이벤트에 현금·보유 수량·메모 원문은 보내지 않는다.

## 12. 화면 설계 후속 결정

- 모바일 중심으로 만들고 PC에서도 사용 가능하게 한다.
- 밝고 간결한 스타일로 설계한다.
- MVP는 직접 입력부터 구현한다. 잔고 캡처 자동 입력은 다음 버전이다.
- 캡처를 추가할 때는 메리츠 증권앱부터 지원하고, 과거·현재 캡처 모두 기준 날짜를 확인한다. 현재 잔고로 과거 보유 내역을 자동 복원하지 않는다.
- MVP 독립 페이지는 둘러보기, 검색, 종목 상세, 가상 매수 입력, 결과·기록 상세, 내 기록의 6개다.
- 세부 레이아웃과 화면 상태는 [[화면설계]]를 따른다.
- 직접 입력은 단계별 이동 없이 한 화면에 날짜·자금·투자금·거래 요약을 배치한다. 확정된 주요 화면 상태는 7개, 보조 UI는 5종이다.
- 관련 종목은 차트 아래 최대 3개 미리보기와 관련 종목 탭을 함께 제공한다.
- 결과 화면에서 현재 예상 잔고를 가장 크게 표시하고 유지 대비 차이를 바로 아래에서 비교한다.

- 종목 상세는 이전 화면 위에 오른쪽에서 쌓이는 화면이다. 왼쪽 위 뒤로 화살표 또는 왼쪽 가장자리에서 오른쪽 스와이프로 한 화면씩 복귀한다. 상세는 오른쪽으로 사라지고 이전 화면의 스크롤·검색·탭·선택 상태를 복원한다.
- 상세의 상단 바와 하단 매수 버튼은 본문 스크롤과 분리한다. 관련 종목에서 연속 진입해도 같은 스택 규칙을 적용한다.
