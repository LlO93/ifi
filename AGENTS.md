# 프로젝트 작업 지침

## 0. 최소 컨텍스트 작업 절차
- 모든 작업은 먼저 `PROJECT_STATUS.md`를 읽고 시작한다. 이 문서가 현재 구현 상태, 최근 검증, 다음 작업, 관련 파일을 요약하는 단일 인계 문서다.
- `PROJECT_STATUS.md`만으로 범위를 정할 수 있으면 기획 문서 전체, 전체 파일 목록, 관련 없는 코드를 다시 읽지 않는다.
- 다음 순서로 필요한 정보만 넓힌다: `PROJECT_STATUS.md` → 그 문서가 지정한 관련 파일·검색어 → 직접 의존 코드 → 상세 기획 문서의 해당 절.
- 파일 구조 전체를 확인하는 명령이나 여러 긴 파일의 일괄 출력은 구조가 바뀌었거나 현황 문서가 부정확하다는 근거가 있을 때만 사용한다.
- 모든 파일 수정 작업이 끝나면 최종 답변 전에 `PROJECT_STATUS.md`를 갱신한다. 확인한 사실, 사용자 결정, 수정 파일, 검증 결과, 남은 작업과 다음 시작점을 현재 상태 기준으로 짧게 덮어쓴다.
- 조사·설명만 하고 프로젝트 상태가 바뀌지 않은 작업은 갱신하지 않아도 된다. 상태가 바뀌었는지 애매하면 갱신한다.
- `PROJECT_STATUS.md`에는 비밀키, 개인정보, 긴 로그, 전체 코드, 대화 경위를 넣지 않는다. 완료된 세부 이력은 누적하지 않고 현재 판단에 필요한 내용만 남긴다.
- 현황 문서는 약 120줄 이하로 유지한다. 길어지면 오래된 완료 내역을 합치거나 제거하고, 상세 규칙은 기존 전용 문서에 링크한다.

## 1. 목표와 작업 범위부터 확인한다
- 이 프로젝트는 미국 주식을 탐색하고, 과거 예수금이나 보유 주식 일부로 다른 종목을 샀을 경우의 현재 예상 잔고를 계산하며, 당시 판단을 기록하는 ‘투자 IF’ 앱이다.
- 작업을 시작할 때 사용자의 요청과 기획 요약을 바탕으로 이번 작업의 목표와 수정 범위를 짧게 정리한다.
- 사용자 요청으로 확정된 사항과 문서의 제안·가정을 구분한다. 제안을 임의로 확정 사항으로 취급하지 않는다.

## 2. 컨텍스트를 필요한 정보 중심으로 구성한다
- 컨텍스트는 AI가 답하거나 작업할 때 참고하는 정보이며, 한 번에 활용할 수 있는 양에는 한계가 있다.
- 긴 코드나 많은 파일을 한꺼번에 읽으면 일부 내용이 입력에서 빠지거나 중요한 내용을 충분히 활용하지 못할 수 있다.
- 먼저 짧은 기획 요약과 파일 구조를 확인하고, 검색으로 이번 작업에 관련된 문서 절·함수·컴포넌트를 찾아 읽는다. 필요한 경우 의존 코드와 호출부까지 범위를 넓힌다.

## 3. 문서와 코드를 역할별로 관리한다
- 프로젝트 목표는 짧은 기획 요약으로 유지하고, 상세 규칙과 화면 설계는 해당 문서에서 관리한다. 같은 내용을 여러 문서에 길게 복사하지 않는다.
- 코드는 역할과 책임에 따라 나눈다. 다만 컨텍스트를 줄이기 위해 관련 없는 코드를 임의로 분리하거나 대규모로 재구성하지 않는다.
- 작업이 길어지면 확인한 사실, 결정 사항, 수정한 파일, 검증 결과, 남은 작업을 짧게 요약해 이어서 작업할 수 있게 한다.
- 작업 간 인계 요약은 `PROJECT_STATUS.md` 한 곳에서 관리한다. `시작아이디어.md`와 `화면설계.md`에 일회성 진행 기록을 반복해서 추가하지 않는다.

## 4. 필요한 파일을 선택해서 확인한다
- 참고 문서:
  - `PROJECT_STATUS.md`: 항상 가장 먼저 읽는 현재 상태·다음 작업 요약.
  - `시작아이디어.md`: 현재 제품 목표와 핵심 흐름.
  - `MVP개발명세.md`: 기능 범위, 계산 규칙, 데이터·구현 명세.
  - `화면설계.md`: 페이지 구성, 레이아웃, 화면별 상호작용.
  - `시작아이디어_초기안.md`: 과거 아이디어 참고용. 현재 요구사항으로 자동 적용하지 않는다.
- 현재 루트의 `index.html`, `style.css`, `app.js`는 최신 6페이지 화면 시안을 역할별로 분리한 파일이다. `js/demo-flow.js`는 고정 가상 가격과 입력값으로 날짜 선택·자금 검증·예상 잔고 비교를 제공하는 데모이며 실제 시세·기업행동을 처리하는 출시용 계산과 구분한다. `js/records.js`는 브라우저 내 데모 기록·메모 저장을 담당하며 서버·계정 저장과 구분한다. 이전 정적 프로토타입은 `archive/날짜-시간/` 아래에 보존한다. 공통 UI 생성 함수는 `js/components.js`에 있으며 `js/components.js` → `js/records.js` → `js/demo-flow.js` → `app.js` → `js/live-market.js` 순서로 로드한다. 실제 시세 탭은 `server/index.mjs`와 `server/market.mjs`의 개인 개발용 서버로 연결하며 키 없는 상태는 공식 IBM 예제 조회다. 실행·키 설정은 `실제데이터연결.md`를 참고한다. 전체 구조는 `component-structure.md`를 참고한다. 파일 구조가 바뀌면 이 안내도 갱신한다.
- 문서 간 충돌이 있으면 최신 사용자 지시와 명시된 확정 사항을 우선 확인한다. 작업 결과에 영향을 주는 모호함만 사용자에게 질문한다.
- 로컬에서 접근 가능한 파일은 AI가 직접 검색하고 읽는다. 사용자에게 프로젝트 전체나 파일 전체를 반복해서 붙여 넣도록 요청하지 않는다.
- 파일에 접근할 수 없는 대화 환경에서는 기획 요약과 파일 구조를 먼저 받고, 필요한 관련 코드만 구체적으로 요청한다.
- 읽지 않은 파일을 읽었다고 가정하거나, 일부만 읽고 전체 구현을 확인했다고 말하지 않는다. 기획에 적힌 기능과 실제 구현된 기능도 구분한다.

## 5. 변경과 답변은 필요한 부분에 집중한다
- 파일 수정이 요청되면 접근 가능한 파일을 직접 수정하고, 요청 범위에 맞는 검증을 수행한다.
- 답변에는 전체 파일을 반복해서 출력하지 않고, 변경한 파일과 핵심 변경 내용, 검증 결과 및 미확인 사항을 간결하게 설명한다.
- 사용자가 직접 적용할 코드를 요청하면 파일 이름, 찾을 기존 코드, 교체·추가할 코드, 정확한 적용 위치를 제공한다. 위치는 줄 번호만으로 설명하지 않고 함수명이나 주변 코드도 함께 제시한다.
- 전체 파일은 사용자가 요청하거나 부분 변경만으로 전달하기 어려운 경우에 제공한다.

## 6. “다음 스텝”은 사용자가 따라가는 수업을 기준으로 안내한다
- 사용자는 [바이브 코딩 가이드](https://aldin0233.github.io/vibe-coding-course-samples/)를 따라 이 프로젝트를 만들고 있다. 직접 공유한 링크는 [GitHub 가이드의 Pages 단계](https://aldin0233.github.io/vibe-coding-course-samples/github/#pages)다. 이 링크만으로 앞 단계가 모두 완료됐거나 현재 진도가 Pages라고 단정하지 않는다.
- 사용자가 “다음 스텝 알려줘”, “다음에 뭐 해?”라고 하면 수업의 해당 단계와 현재 프로젝트 상태를 비교하여 아직 끝내지 않은 단계부터 안내한다. 일반적인 개발 로드맵을 임의로 대신 제시하지 않는다.
- 가이드는 바이브 코딩(컨텍스트·기획·파일과 컴포넌트 분리), GitHub, VS Code, Supabase, Vercel로 구성된다. 카테고리 나열을 필수 진행 순서로 가정하지 말고 실제 수업의 순서와 분기를 확인한다.
- GitHub 가이드의 순서는 저장소 생성 버튼 찾기 → 저장소 만들기 → 파일·폴더 업로드 → 폴더와 파일 만들기 → GitHub Pages → 파일·ZIP 다운로드다. 필요한 단계인지도 함께 판단한다.
- 가이드의 Pages 단계는 기본 HTML 프로젝트용이다. Vite·Supabase 수업은 가이드가 안내하는 Vercel 경로를 따른다. 프로젝트의 실제 구성과 사용자가 진행 중인 수업을 확인해 적용한다.
- 각 단계는 `완료 / 일부 완료 / 미완료 / 확인 필요 / 해당 없음`으로 구분한다. 파일·실행 결과·서비스 설정·사용자의 완료 확인을 근거로 판단하고, 근거가 없으면 미완료가 아닌 확인 필요로 둔다.
- 기획 문서나 SQL 문서가 있다는 것만으로 기능 구현이나 DB 적용이 완료됐다고 판단하지 않는다. 로컬 파일 존재와 GitHub 업로드·배포 완료도 구분한다.
- 접근 가능한 파일과 설정은 직접 확인한다. 저장소 주소나 외부 서비스 상태 등 확인할 수 없는 정보가 다음 단계 결정에 꼭 필요할 때만 최소한으로 질문한다. 도구 실행 실패를 사용자의 학습 단계 미완료로 해석하지 않는다.
- 답변은 ① 현재 확인된 진도 ② 가장 먼저 남은 단계와 판단 근거 ③ 지금 할 구체적인 행동 ④ 완료 확인 방법과 해당 수업 링크 순으로 간결하게 안내한다. 처음부터 모든 단계를 다시 설명하지 않는다.
- “다음 스텝 알려줘”는 진도 확인과 안내 요청으로 취급한다. 실제 구현·설정 변경·업로드·배포는 사용자의 작업 요청 범위에 따라 수행한다.
- 이후 완료한 단계는 근거와 함께 짧게 기록해 다음 안내에 활용하고, 오래된 기록보다 현재 상태와 최신 사용자 설명을 우선한다. 가이드에 접근하지 못하면 확인하지 않은 수업 내용을 추측하지 않는다.
