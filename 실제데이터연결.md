# 실제 주가 연결 — 개인 개발 검증

## 현재 범위

- Alpha Vantage의 미국 주식 검색과 일별 원시 종가 API를 연결한다.
- 무료 일별 데이터는 최근 100개 자료로 제한된다. 당일 자료는 확정 여부를 보수적으로 판단해 제외하고 수신한 마지막 거래일을 표시한다. 실시간 시세가 아니다.
- API 키가 없으면 공급자의 공식 IBM 예제 API만 사용할 수 있다. 이는 가짜로 생성한 가격이 아니라 공급자 예제 응답이며 최신성은 보장하지 않는다.
- 실제 가격 조회와 기존 가상 종목 계산은 별도 탭이다. 실제 데이터로 투자 잔고를 계산하려면 분할·합병·상장폐지 등 기업행동과 공통 평가일 검증을 추가해야 한다.
- 첫 목록은 조회 시작용 종목이다. 거래량·시가총액 순위나 업종 추천은 아직 연결하지 않았다.

## 실행

Node.js 22 이상에서 프로젝트 폴더를 열고 `npm start`를 실행한다. 추가 패키지 설치는 필요 없다. 접속 주소는 [로컬 앱](http://127.0.0.1:4173)이다.

프로젝트는 Vite 빌드 구조다. 의존성 설치 후 `npm run build`를 실행하면 배포용 `dist/`가 생성된다. 수업 흐름에서는 로컬 명령 대신 Vercel이 이 빌드를 수행한다. `npm start`는 기존 개인 개발용 시세 서버와 화면을 함께 확인하는 명령으로 유지한다.

파일을 직접 여는 `file://` 방식에서는 비밀키를 숨길 서버가 없어 실제 시세를 요청하지 않는다. 로컬 서버는 이 컴퓨터에서만 접속할 수 있고 공개 배포용 서버가 아니다.

## 다른 종목을 조회하려면

1. [Alpha Vantage 무료 키 발급 페이지](https://www.alphavantage.co/support/#api-key)에서 개인 개발용 키를 발급받는다. 계정 생성·약관 동의는 직접 진행한다.
2. `.env.example`을 `.env.local`로 복사한다.
3. `ALPHA_VANTAGE_API_KEY=` 오른쪽에 키를 넣고 저장한다. 키는 채팅에 보내거나 GitHub에 올리지 않는다.
4. 서버를 다시 실행하고 화면을 새로고침한다. ‘개인 API 키 연결’ 표시를 확인한 뒤 AAPL 등을 선택한다.

`.env.local`은 Git 제외 대상이고 서버의 정적 파일 허용 목록에도 없다. 화면과 API 응답에는 키를 보내지 않는다.

## 호출 한도와 오류

- 검색 버튼이나 종목을 눌러야 공급자 API를 요청한다. 이미 받은 응답은 서버 메모리에서 1시간 재사용한다. 동일 요청은 합쳐서 처리한다.
- 서버는 UTC 하루 최대 25회 신규 호출로 제한한다. 재시작하면 내부 카운트가 초기화되지만 공급자의 계정 한도는 초기화되지 않는다. 개발용 보조 제한이며 공급자 한도 오류를 별도로 표시한다.
- 서버가 응답하지 않거나 키·한도 문제가 있으면 오류와 재시도를 표시한다. 실패를 가짜 주가로 대체하지 않는다.
- 오래된 자료도 날짜를 명확히 표시하며 ‘오늘 가격’이나 ‘실시간’이라고 부르지 않는다.
- `file://`에서 저장한 데모 기록과 `http://127.0.0.1:4173`에서 저장한 기록은 브라우저 저장 영역이 달라 자동으로 공유되지 않는다. 기존 기록은 삭제하지 않는다.

## 출시 전

이번 연결은 개인 무료 개발 검증이다. 앱인토스 등 공개 서비스에서의 데이터 표시·계산·보관 권한과 비용은 별도 확인해야 한다.

공식 참고: [API 문서](https://www.alphavantage.co/documentation/) · [개인 이용 약관](https://www.alphavantage.co/terms_of_service/) · [무료 API 안내](https://www.alphavantage.co/support/)
